en:
  activerecord:
    models:
      fields/boolean_field: Boolean field
      fields/text_field: Text field
      fields/decimal_field: Decimal field
      fields/integer_field: Integer field
      fields/date_field: Date field
      fields/datetime_field: Datetime field
      fields/select_field: Select field
      fields/index_select_field: Index select field
      fields/multiple_select_field: Multiple select field
      fields/resource_field: Resource field
      fields/resource_select_field: Resource select field
      fields/nested_form_field: Nested form field
      fields/variable_length_nested_form_field: Variable length nested form field
    attributes:
      field:
        name: Name
        label: Label
        hint: Hint
        accessibilities:
          hidden: Hidden
          readonly: Read-only
          read_and_write: Normal
  duck_record:
    attributes:
      field_options:
        presence: Required
        acceptance: Acceptance
        confirmation: Confirmation
        exclusion: Exclusion
        format: Format
        inclusion: Inclusion
        length: Length
        numericality: Numericality
        message: Message
        with: Regexp
        in: In
        minimum: Minimum
        maximum: Maximum
        is: Equal
        lower_bounds:
          disabled: 'Off'
          greater_than: '>'
          greater_than_or_equal_to: '>='
        upper_bounds:
          disabled: 'Off'
          less_than: '<'
          less_than_or_equal_to: '<='

