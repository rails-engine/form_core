<div class="field">
  <div class="control">
    <label class="checkbox">
      <%= f.check_box field.name, id: field.id, disabled: 'disabled' %>
      <%= field.label %>
    </label>
  </div>
  <% if field.hint.present? %>
    <p class="help"><%= field.hint %></p>
  <% end %>
</div>
